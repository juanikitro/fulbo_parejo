# Custom teams mode

Balanced matchmaking remains the default. A secondary custom mode lets users select one player from each team and swap them without rating or position constraints. Team sizes remain equal so match recording and Elo remain valid. The original balanced proposal remains available through a restore action.
