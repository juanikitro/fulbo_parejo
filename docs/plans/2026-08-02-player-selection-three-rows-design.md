# Player selection: three rows

## Goal

Show the player call-up chips in three rows instead of two.

## Design

Keep the existing horizontally scrollable, column-flowing grid and its chip sizing. Change only the row count from two to three, so selection behavior, accessibility, and the call-up data flow remain unchanged.

## Validation

Run the existing build/type-check command.
